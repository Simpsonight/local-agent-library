### TASK: SURGICAL SEO OPTIMIZATION
Revise the text based on the keywords. Strictly apply the marker rules from the system prompt.

**Target Keywords:** {{ keywords }}

**Additional Strategy Input (optional):**
{{ analysis_plan }}

**Original Text to Edit:**
---
{{ original_text }}
---

**IMPORTANT:** Keep the structure (Markdown/HTML) exactly intact. The optimized text MUST remain in the same language as the original text.

**Expected Output:**
Respond with a JSON object containing the following fields:
- `optimized_text`: The complete optimized text with [[NEW: ...]] and [[CHANGED: ...]] markers. Keep the text in its original language — only add/modify content, do not translate.
- `changes`: Array of objects with `type` ("new" or "changed"), `location`, `description` — list of all changes. Write `location` and `description` in English.
- `keywords_integrated`: Array of keywords successfully integrated (in their original language)