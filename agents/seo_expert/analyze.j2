### TASK: SEO ANALYSIS & MAPPING
Analyze the following text and create a plan for where the target keywords can be most effectively integrated without overloading the text.

**Target Keywords:** {{ keywords }}

**Original Text:**
---
{{ original_text }}
---

**Expected Output:**
Respond with a JSON object containing the following fields:
- `current_keywords`: Array of keywords already present in the text (keep them in the original language of the text)
- `missing_keywords`: Array of target keywords not yet present (keep them in the original language)
- `optimization_locations`: Array of objects with `location`, `suggestion`, `keyword` — specific places with optimization potential. Write `location` and `suggestion` in English.
- `semantic_gaps`: Array of missing semantic topics or related terms (in English)
- `overall_score`: Number 0-100 for the current SEO optimization score